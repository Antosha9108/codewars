function maps(x){
    map1 = x.map(element => element*2)
      return map1
    } 


    const zeroFuel = (distanceToPump, mpg, fuelLeft) => {
        if (distanceToPump => Number(mpg*fuelLeft)){
          return true
        }else {
          return false
        }
        // TODO
      };